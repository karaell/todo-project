export const PRIORITY_BLOCKS = document.querySelectorAll(".todo__priority-block");
export const ADD_INPUTS = document.querySelectorAll(".input-add");
export const ADD_BTNS = document.querySelectorAll(".btn-add");

export let tasks = document.querySelectorAll(".todo__task");
export let deleteBtns = document.querySelectorAll(".btn-delete");
export let checkboxDone = document.querySelectorAll(".input-done"); 